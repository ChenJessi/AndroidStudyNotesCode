<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <TextView
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@drawable/image_head"
        android:gravity="center"
        android:text="Behavior的嵌套滑动展示"
        android:textColor="@color/color_bg"
        app:layout_behavior=".materialdesign.coordinator.behavior.SampleHeaderBehavior"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior=".materialdesign.coordinator.behavior.ScrollerBehavior"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>